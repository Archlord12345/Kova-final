{
  "version": 2,
  "installCommand": "if [ -f server/package-lock.json ]; then npm ci --prefix server; else npm install --prefix server; fi",
  "buildCommand": "echo 'KOVA backend: no build step required'",
  "builds": [
    {
      "src": "server/src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/src/index.js"
    }
  ]
}
