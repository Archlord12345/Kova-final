[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["if [ -f server/package-lock.json ]; then npm ci --omit=dev --prefix server; else npm install --omit=dev --prefix server; fi"]

[phases.build]
cmds = []

[start]
cmd = "npm start --prefix server"
